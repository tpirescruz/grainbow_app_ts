export interface Person {
    id: number
    name: string;
    lastName: string;
    age?: number;
    height?: number; // in centimeters
}

export type SVGProps = {
    size?: string,
    color?: string,

}

